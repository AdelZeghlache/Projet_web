{# src/RETROG/UserBundle/Resources/views/Security/login.html.twig #}

{% extends "::layout.html.twig" %}

{% block body %}
  
  <p>La plateforme est réservée aux employés de l'entreprise, si vous n'avez pas vos identifiants veuillez contacter <a> l'administrateur </a>.

  {# S'il y a une erreur, on l'affiche dans un joli cadre #}
  {% if error %}
    <div class="alert alert-danger">{{ error.message }}</div>
  {% endif %}

  {# Le formulaire, avec URL de soumission vers la route « login_check » comme on l'a vu #}

<div id="loginForm">
  <div class="row">
      <div class="col-xs-12">
          <div class="well">
              <form id="loginForm" action="{{ path('login_check') }}" method="POST">
                  <div class="form-group">
                      <label for="username" class="control-label">Username</label>
                      <input type="text" class="form-control" name="_username" id="username" value="{{ last_username }}" required="" title="Please enter your username" placeholder="username">
                      <span class="help-block"></span>
                  </div>
                  <div class="form-group">
                      <label for="password" class="control-label">Mot de passe</label>
                      <input type="password" class="form-control" name="_password" id="password" placeholder="password" value="" required="" title="Please enter your password">
                      <span class="help-block"></span>
                  </div>
                  <button type="submit" value="Connexion" class="btn btn-success btn-block">Connexion</button>
              </form>
          </div>
      </div>
  </div>
</div>

{% endblock %}